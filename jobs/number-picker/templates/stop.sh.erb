#!/bin/bash

echo "Stopping number picker job"

RUN_DIR=/var/vcap/sys/run/number-picker
LOG_DIR=/var/vcap/sys/log/number-picker

/sbin/start-stop-daemon \
  --pidfile "${RUN_DIR}/pid" \
  --remove-pidfile \
  --retry TERM/20/QUIT/1/KILL \
  --oknodo \
  --stop